{
  "name": "confiterias-viegener",
  "private": true,
  "description": "Confiterías Viegener — the website in website/ and this repository's own safety guard",
  "type": "module",
  "scripts": {
    "check": "node scripts/check-repository.mjs",
    "test": "node --test \"tests/*.test.mjs\"",
    "preflight": "npm run check && npm test"
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
