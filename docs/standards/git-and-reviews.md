# Git and reviews

Use a focused branch and pull request; never push directly to `main`. Checks
must be green for the current head; resolve review threads and do not rely on
stale approval after a material change. Never weaken a check in the same pull
request that needs it to pass.

Keep unrelated changes out of a pull request. Merges and deployment remain human
decisions.
